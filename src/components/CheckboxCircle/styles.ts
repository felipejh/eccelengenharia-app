import styled from 'styled-components/native';
import colors from '~/styles/colors';

export const Circle = styled.View`
  border: 5px solid ${colors.orange_light};
  background-color: ${colors.black_light};
  border-radius: 35px;
  width: 35px;
  height: 35px;
`;
